<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Adeptstack_App.Changelogs"
             BackgroundColor="#020617"
             Title="Adeptstack Changelogs">

    <Grid>
        <!-- Layer 1: Content -->
        <RefreshView x:Name="refresh" Refreshing="RefreshView_Refreshing" RefreshColor="#3b82f6">
            <ScrollView>
                <Grid Padding="16,24,16,24">
                    <FlexLayout x:Name="appsLayout" 
                        Wrap="Wrap" 
                        JustifyContent="Center" 
                        AlignItems="Start"
                        AlignContent="Start"
                        MaximumWidthRequest="900" />
                </Grid>
            </ScrollView>
        </RefreshView>

        <!-- Layer 2: Empty / Error State -->
        <Grid x:Name="nothing" IsVisible="False" BackgroundColor="#020617">
            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="8">
                <Label Text="No Apps Available" 
                       TextColor="#f8fafc" 
                       FontSize="20" 
                       FontAttributes="Bold" 
                       HorizontalTextAlignment="Center" />

                <Label x:Name="internet" 
                       Text="Please check your internet connection." 
                       TextColor="#94a3b8" 
                       FontSize="14" 
                       IsVisible="False" 
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Grid>

        <!-- Layer 3: Loading State -->
        <Grid x:Name="loading" BackgroundColor="#CC020617" IsVisible="False">
            <ActivityIndicator x:Name="busy" 
                               IsRunning="True" 
                               Color="#3b82f6" 
                               HorizontalOptions="Center" 
                               VerticalOptions="Center" />
        </Grid>
    </Grid>
</ContentPage>